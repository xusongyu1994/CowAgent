---
name: 权限管理页面设计与实现
overview: 为 CowAgent 项目新增权限管理页面，管理企微用户访问知识库的权限和使用金蝶的权限。基于现有的 permission_config.json 配置文件和 wecom_user_details.json 用户数据，新增前端页面和后端 API。
design:
  architecture:
    framework: html
  styleKeywords:
    - Dark Mode
    - Admin Panel
    - Data Table
    - Toggle Switch
    - Enterprise
  fontSystem:
    fontFamily: Inter
    heading:
      size: 20px
      weight: 600
    subheading:
      size: 16px
      weight: 500
    body:
      size: 14px
      weight: 400
  colorSystem:
    primary:
      - "#35A85B"
      - "#228547"
      - "#1C6B3B"
    background:
      - "#111111"
      - "#1A1A1A"
      - "#0A0A0A"
    text:
      - "#FFFFFF"
      - "#E2E8F0"
      - "#94A3B8"
    functional:
      - "#35A85B"
      - "#EF4444"
      - "#F59E0B"
      - "#3B82F6"
todos:
  - id: add-backend-api
    content: 在web_channel.py中添加权限管理API端点和Handler类
    status: pending
  - id: add-frontend-view
    content: 在chat.html中添加权限管理视图页面和侧边栏菜单项
    status: pending
    dependencies:
      - add-backend-api
  - id: add-frontend-logic
    content: 在console.js中添加权限管理前端逻辑和视图切换支持
    status: pending
    dependencies:
      - add-frontend-view
  - id: add-i18n-text
    content: 在console.js的I18N对象中添加权限管理相关多语言文本
    status: pending
    dependencies:
      - add-frontend-logic
  - id: test-permissions-page
    content: 测试权限管理页面功能，验证用户列表加载、权限切换和配置保存
    status: pending
    dependencies:
      - add-i18n-text
---

## 产品概述

为揽盛电气智能体控制台新增权限管理页面，用于管理企微用户访问知识库和使用金蝶查询功能的权限。

## 核心功能

1. **企微用户管理**：展示所有企微用户列表，支持查看用户姓名、别名、部门信息
2. **知识库权限管理**：管理哪些企微用户可以访问知识库，支持单个用户授权/撤销、批量操作
3. **金蝶权限管理**：管理哪些企微用户可以使用金蝶查询功能，支持单个用户授权/撤销、批量操作
4. **权限配置持久化**：将权限配置保存到`tmp/permission_config.json`文件
5. **用户数据同步**：从企微获取最新用户数据并更新`tmp/wecom_user_details.json`

## 数据来源

- 用户数据：`tmp/wecom_user_details.json`（已有47个用户的数据）
- 权限配置：`tmp/permission_config.json`（已有知识库和金蝶权限结构）

## 技术栈选择

- **前端**：HTML + Tailwind CSS + Vanilla JavaScript（与现有chat.html保持一致）
- **后端**：Python web.py框架（与现有web_channel.py保持一致）
- **数据存储**：JSON文件（与现有配置存储方式保持一致）

## 实施方案

### 1. 后端API设计

在`channel/web/web_channel.py`中添加以下API端点：

```
GET  /api/permissions                      # 获取权限配置
PUT  /api/permissions                      # 更新权限配置
GET  /api/permissions/users               # 获取企微用户列表
POST /api/permissions/knowledge          # 更新知识库权限
POST /api/permissions/kingdee           # 更新金蝶权限
POST /api/permissions/sync-users        # 同步企微用户数据
```

### 2. 后端Handler实现

新增`PermissionsHandler`类，包含以下方法：

- `GET()` - 返回当前权限配置和用户列表
- `PUT()` - 更新整个权限配置
- 子路径处理方法（通过解析path实现）

权限数据操作封装在独立的函数中：

- `_load_permission_config()` - 加载权限配置
- `_save_permission_config(data)` - 保存权限配置
- `_load_wecom_users()` - 加载企微用户数据
- `_sync_wecom_users()` - 从企微API同步用户数据

### 3. 前端页面设计

在`chat.html`中添加：

- 侧边栏菜单项：在"管理"组下添加"权限"菜单项
- 视图容器：添加`view-permissions`视图
- i18n文本：添加权限管理相关的多语言文本

在`console.js`中添加：

- VIEW_META注册：添加`permissions`视图元数据
- 导航钩子：添加权限视图的懒加载逻辑
- 权限管理功能函数：
- `loadPermissionsView()` - 加载权限管理页面
- `renderUserTable()` - 渲染用户表格
- `toggleKnowledgePermission()` - 切换知识库权限
- `toggleKingdeePermission()` - 切换金蝶权限
- `savePermissions()` - 保存权限配置
- `syncWecomUsers()` - 同步企微用户

### 4. URL路由注册

在`web_channel.py`的`urls`元组中注册新路由：

```python
'/api/permissions', 'PermissionsHandler',
'/api/permissions/(.*)', 'PermissionsHandler',
```

## 实现注意事项

### 性能考虑

- 用户列表数据量小（当前47用户），一次性加载即可
- 权限配置读写使用文件锁避免并发问题
- 企微用户同步操作可能较慢，需要显示加载状态

### 安全考虑

- 所有API端点都需要`_require_auth()`认证检查
- 权限配置文件路径使用绝对路径，防止路径遍历
- 用户ID输入需要验证，防止注入攻击

### 错误处理

- API调用失败时需要显示用户友好的错误提示
- 文件读写失败需要记录日志并通知用户
- 企微API调用失败时需要重试机制

## 目录结构

```
channel/web/
├── chat.html                          [MODIFY] 添加权限管理视图和菜单项
├── static/js/console.js               [MODIFY] 添加权限管理前端逻辑
└── web_channel.py                     [MODIFY] 添加权限管理API端点
tmp/
├── permission_config.json              [EXISTING] 权限配置文件（已存在）
└── wecom_user_details.json            [EXISTING] 企微用户数据文件（已存在）
```

## 设计风格

采用与现有控制台一致的设计风格，使用暗色主题（dark mode），保持界面统一性和专业性。

## 页面布局

### 权限管理页面整体结构

页面分为两个主要功能区，使用标签页（Tabs）切换：

1. **知识库权限**标签页
2. **金蝶权限**标签页

### 页面头部区块

- 标题："权限管理"
- 描述："管理企微用户访问知识库和使用金蝶的权限"
- 操作按钮：
- "同步用户"按钮（从企微刷新用户列表）
- "保存配置"按钮（保存权限修改）

### 用户列表表格区块

表格包含以下列：

- 复选框（支持批量操作）
- 用户姓名
- 用户别名/UserID
- 部门
- 知识库权限状态（开关切换）
- 金蝶权限状态（开关切换）

### 批量操作栏区块

- 显示已选用户数量
- "批量授权知识库"按钮
- "批量撤销知识库"按钮
- "批量授权金蝶"按钮
- "批量撤销金蝶"按钮

### 交互设计

- 使用Toggle开关组件切换权限状态
- 权限变更后自动保存到后端（防抖处理，延迟1秒）
- 表格支持按部门筛选（下拉选择）
- 表格支持按姓名搜索（输入框实时筛选）

## 视觉规范

- 保持与现有控制台一致的暗色主题
- 使用Primary绿色（#35A85B）表示已授权状态
- 使用灰色表示未授权状态
- 表格行悬停效果：background-color: rgba(255,255,255,0.05)
- 开关组件样式与Tailwind CSS兼容